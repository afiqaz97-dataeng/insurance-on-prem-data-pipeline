name: 'dbt_takaful'
version: '1.0.0'
config-version: 2

profile: 'dbt_takaful'

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
analysis-paths: ["analyses"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  dbt_takaful:
    staging:
      +materialized: view
    marts:
      +materialized: table
